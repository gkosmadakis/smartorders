<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.OrderProgressActivity">

    <TextView
        android:id="@+id/orderProgressTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Order Progress"
        android:textSize="20sp"
        android:layout_marginTop="40dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/countDown"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="50sp"
        android:textStyle="bold"
        android:layout_marginTop="40dp"
        app:layout_constraintEnd_toStartOf="@+id/orderProgressTime"
        app:layout_constraintStart_toEndOf="@+id/orderProgressTime"
        app:layout_constraintTop_toBottomOf="@+id/orderProgressTime" />

    <Button
        android:id="@+id/homeButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:background="#3F51B5"
        android:text="Continue Browsing"
        android:textColorLink="#090909"
        app:layout_constraintEnd_toStartOf="@+id/countDown"
        app:layout_constraintStart_toEndOf="@+id/countDown"
        app:layout_constraintTop_toBottomOf="@+id/countDown" />

</androidx.constraintlayout.widget.ConstraintLayout>